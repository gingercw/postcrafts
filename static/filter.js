// fetch(`/search_templates`) 
// .then((response) => response.json())
// .then((templates) => {
  

//   for (const template of templates) {
//     const berryName = responseData.results[i].name;
//     document.querySelector('#berries').insertAdjacentHTML('beforeend', `${berryName}, `);

//   <form action="/savetemplate/{{template.card_id}}" method="POST">
//   <h3>{{template.title}}</h3>
//   <img src="{{ template.url }}" width="300" />
//   <button name="save_template">Add to Your Cards</button>
// </form>

  
//   }

// })

